# Leave unset for local dev (defaults to http://localhost:5000/api and ws://localhost:5000)
# Set to your deployed backend URL + /api when deploying to Vercel
VITE_API_URL=https://your-backend-name.onrender.com/api
